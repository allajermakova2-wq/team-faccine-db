<style>
  .legal-container {
    background-color: #000000;
    color: #ff0000;
    font-family: 'Courier New', Courier, monospace;
    padding: 15px; /* Уменьшил внутренний отступ */
    border: 1px double #330000;
    line-height: 1.3; /* Сделал текст плотнее */
    text-align: left;
  }

  .header-yellow {
    color: #ffd700;
    font-size: 18px; /* Чуть уменьшил, чтобы не занимал много места */
    font-weight: bold;
    text-transform: uppercase;
    text-shadow: 0 0 8px #ffd700;
    display: block;
    margin-bottom: 15px; /* Сжал отступ под заголовком */
    border-bottom: 1px solid #ffd700;
    padding-bottom: 5px;
  }

  .glow-text {
    text-shadow: 0 0 5px #ff0000;
    font-weight: bold;
    white-space: pre-wrap;
    display: block;
    font-size: 14px; /* Компактный размер шрифта */
  }

  /* Минимальные отступы между абзацами */
  .p-min {
    margin-bottom: 10px;
  }

  .timer-section {
    margin-top: 15px;
    border-top: 1px solid #333;
    padding-top: 10px;
    color: #00ff00; 
    font-size: 18px;
    font-weight: bold;
  }

  #countdown {
    letter-spacing: 1px;
    text-shadow: 0 0 5px #00ff00;
  }
</style>

<div class="legal-container">
  <div class="header-yellow">
    Esercizio dei diritti ex artt. 15-22 Regolamento UE 2016/679 (GDPR) – Contratto dal 01/07/2024.
  </div>

  <div class="glow-text">
    <div class="p-min">Spettabile Servizio Privacy Enel Energia,</div>

    <div class="p-min">con la presente, io sottoscritta ALLA JERMAKOVA, intendo esercitare i miei diritti GDPR per il contratto dal 01/07/2024.</div>

    <div class="p-min">Considerando le gravi irregolarità (Pratica ARERA n. 197857), richiedo:</div>

    <div style="margin-bottom: 5px;">1. L'Accesso a tutti i dati e consensi.</div>
    <div style="margin-bottom: 5px;">2. La Limitazione del trattamento (art. 18 GDPR).</div>
    <div style="margin-bottom: 5px;">3. La Cancellazione (Diritto all'oblio) dei dati non obbligatori.</div>

    <div class="p-min" style="margin-top: 10px;">Godo di tutele rafforzate (disabilità 80%). Segnalerò ogni ritardo al Garante Privacy.</div>

    <div class="p-min">In attesa di conferma (30 giorni).</div>

    <div style="margin-top: 15px; font-size: 16px; color: #ff0000; text-decoration: underline;">ALLA JERMAKOVA</div>
  </div>

  <div class="timer-section">
    <span style="color: #ff0000; font-size: 14px;">START: 20.03.2026, 01:51 |</span>
    <span id="countdown">00.00.00.00</span>
  </div>
</div>

<script>
  const targetDate = new Date(2026, 2, 20, 1, 51, 0).getTime();
  function updateTimer() {
    const now = new Date().getTime();
    const distance = now - targetDate;
    if (distance < 0) { document.getElementById("countdown").innerHTML = "00.00.00.00"; return; }
    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);
    const d = String(days).padStart(2, '0');
    const h = String(hours).padStart(2, '0');
    const m = String(minutes).padStart(2, '0');
    const s = String(seconds).padStart(2, '0');
    document.getElementById("countdown").innerHTML = d + "." + h + "." + m + "." + s;
  }
  setInterval(updateTimer, 1000);
  updateTimer();
</script>